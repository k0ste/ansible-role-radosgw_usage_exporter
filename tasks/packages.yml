---
- name: "rgw_exporter | Install the required packages in Redhat derivatives"
  when:
    - "hostvars[inventory_hostname]['ansible_os_family'] == 'RedHat'"
  block:
    - name: "rgw_exporter | Update dnf cache"
      ansible.builtin.dnf:
        update_cache: "yes"
    - name: "rgw_exporter | Install package with dnf"
      ansible.builtin.dnf:
        name: "{{ hostvars[inventory_hostname]['rgw_exporter_package'] }}"
        state: "present"
- name: "rgw_exporter | Install the required packages in Debian derivatives"
  when:
    - "hostvars[inventory_hostname]['ansible_os_family'] == 'Debian'"
  block:
    - name: "rgw_exporter | Update apt cache"
      ansible.builtin.apt:
        update_cache: "yes"
    - name: "rgw_exporter | Install package with apt"
      ansible.builtin.apt:
        name: "{{ hostvars[inventory_hostname]['rgw_exporter_package'] }}"
        state: "present"
- name: "rgw_exporter | Install the required packages in ArchLinux derivatives"
  when:
    - "hostvars[inventory_hostname]['ansible_os_family'] == 'Archlinux'"
  block:
    - name: "rgw_exporter | Update pacman cache"
      community.general.pacman:
        update_cache: "yes"
    - name: "rgw_exporter | Install package with pacman"
      community.general.pacman:
        name: "{{ hostvars[inventory_hostname]['rgw_exporter_package'] }}"
        state: "present"
